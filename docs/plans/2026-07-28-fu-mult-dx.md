# fu_mult_dx — Implementation Plan

**Spec:** `docs/specs/2026-07-28-fu-mult-dx-design.md`

TDD; same gate (`verilator --lint-only -Wall` clean **and** `PASS:`). DesignWare auto-detected by
`run.sh` (adds the sim_ver library + `tb/dw_waivers.vlt`).

## Steps

1. **Spec** (done).
2. **RED** — `tb/tb_fu_mult_dx.sv` (native-SV multiply-low golden, 1×64 + 2×32, mode-isolation +
   handshake + 20k random) against a **stub** (`out_data = a ^ b`, no DW). Expect lint clean, sim FAIL.
3. **GREEN** — `DW_mult_dx #(.width(64), .p1_width(32))` + low-bit slice per mode. Expect `PASS:`.
4. **README** — add `fu_mult_dx` section (64/32 DW family).
5. **Commit series** (WillWheatley27, subject-only, no AI traces): docs → RED → GREEN → README; push.
6. **Synthesize** SAED14nm both corners with `dw_foundation.sldb` (speed 0.10 ns; area comfortable
   ~0.80 ns — multiply is the slow datapath). Record corners + note (DW `_dx` duplex, multiply-low).
7. **Chart** — append `mult_dx` as a new bottom row of the 64/32 DW family chart.

## Checks

- **Duplex product packing** validated in a probe: `dplx=1` → low lane full product in `prod[63:0]`,
  high lane in `prod[127:64]`; multiply-low = `{prod[95:64], prod[31:0]}`.
- **Verilator + DW_mult_dx** model compiles/simulates (probe confirmed).
- `tc=0` gives correct low bits (sign-agnostic multiply-low).
